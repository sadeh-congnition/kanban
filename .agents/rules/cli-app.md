---
trigger: always_on
---

Use the Python `rich` package to render the CLI user interface.
Implement the CLI command using the Python `django-click` package documented here: https://github.com/django-commons/django-click